# events {}

# http {
#     include       /etc/nginx/mime.types;
#     default_type  application/octet-stream;

#     resolver 8.8.8.8 ipv6=off;

#     server {
#         listen 80;
#         server_name localhost;

#         location / {
#             root /usr/share/nginx/html;
#             index index.html index.htm;
#             try_files $uri $uri/ /index.html;
#         }

#         location /api/ {
#             # FIX: was "backend-service:3001" — corrected to match k8s service name and port
#             set $backend "backend-svc:3000";
#             proxy_pass http://$backend;

#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;

#             error_page 502 503 504 = @backend_down;
#         }

#         location @backend_down {
#             default_type application/json;
#             return 200 '{"error":"Backend unavailable","message":"Please try again later."}';
#         }
#     }
# }

events {}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    server {
        listen 80;
        server_name localhost;

        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
            try_files $uri $uri/ /index.html;
        }

        location /api/ {
            proxy_pass http://backend-svc:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
